1.（）的危险性，通过不予实现避免了。 危险主要在于参数不同导致，会出现两种问题：一个是无限循环，一个是内存溢出数据出错。
2.迭代器M_node的危险性，通过友元避免了
3.空begin（）--和++的错误
(--由对header->parent->parent这句话导致的错误，因为这是header->parent为空)，通过特判避免了
(++是因为这个时候迭代器只能是end(),而header这个时候左右指针都指向自己，所以导致无限循环)，也是通过特判避免了
4.multimap中count函数没有像stl一样使用std::distance函数，而是用另一种写法，原因是distance最差时间复杂度是 O(n) ,而使用get_rank函数可以稳定获得O(logn)的时间复杂度。
5.不仅封装了map，也封装了multimap。已经通过修改底层的rb_treev2.0中的节点信息实现的、


