#include <iostream>
#include <cmath>
#include <string>
#include <cstring>
#include <vector>
#include <algorithm>

#define ll long long

using namespace std;

//operator== 有两种定义方式：
// 一个是类内定义:
// 一个是类外定义：全局函数
// 在红黑树课设中使用了类内定义，但是一般推荐全局函数的方法
// operator == (const class& x)const{}
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0),cout.tie(0);

    


    cout<<endl;
    system("pause");
    return 0;
}
